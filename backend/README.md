# Backend

Flask API for ingesting stock data, exposing analytics endpoints, and orchestrating grounded AI responses.

## Local Setup

```bash
poetry install
poetry run flask --app app run --debug
```
